name = "ai-career-companion-worker"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# 明文变量（非机密）
[vars]
SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_ANON_KEY = "your-anon-key"

# 机密用 secret：wrangler secret put DEEPSEEK_API_KEY
