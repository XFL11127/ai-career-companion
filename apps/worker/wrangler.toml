name = "ai-career-companion-worker"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# Workers AI 绑定：bge-m3 向量模型（1024 维，语义搜索用）
[ai]
binding = "AI"

# 明文变量（非机密）
[vars]
SUPABASE_URL = "https://lyrsoxespiikxaptifjn.supabase.co"
SUPABASE_ANON_KEY = "sb_publishable_FkIJV_6sCrdu963GRSJdcA_BeREBV1p"

# 机密用 secret（部署前执行）：
#   npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY   # Supabase → Settings → API → service_role（sb_secret_ 开头）
#   npx wrangler secret put DEEPSEEK_API_KEY            # 可选，无 key 时 LLM 走 stub 兜底
